# 依賴套件
#
# 安裝方式：
#   pip install -r requirements.txt
#
# 如果只想先看網頁介面（不訓練模型），可以只裝「網頁介面」和「數據處理」
# 這兩個區塊的套件，其他的都可以先跳過 ── PyTorch 相關套件加起來超過 2GB。

# ---------------------------------------------------------------- #
# 數據處理（必要）
# ---------------------------------------------------------------- #
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ---------------------------------------------------------------- #
# 網頁介面（必要）
# ---------------------------------------------------------------- #
# 介面用到 width="stretch"（1.49 才有，取代已棄用的 use_container_width）
streamlit>=1.49.0
plotly>=5.18.0

# ---------------------------------------------------------------- #
# 深度學習框架（訓練模型才需要）
# ---------------------------------------------------------------- #
torch>=2.0.0
torchvision>=0.15.0
torch-geometric>=2.3.0

# ---------------------------------------------------------------- #
# 多模態處理（訓練模型才需要）
# ---------------------------------------------------------------- #
transformers>=4.30.0
sentence-transformers>=2.2.0
pillow>=10.0.0

# ---------------------------------------------------------------- #
# 交通模擬（選用）
#
# 注意：PyPI 上的 `sumo` 套件跟交通模擬器 SUMO 完全無關，裝了也沒用。
# 正確的套件叫 `eclipse-sumo`，它會一併提供 `traci`。
# 沒有安裝也沒關係 ── 程式會自動改用內建的模擬環境。
# 官方安裝說明：https://eclipse.dev/sumo/
# ---------------------------------------------------------------- #
eclipse-sumo>=1.18.0 ; sys_platform != "darwin"

# ---------------------------------------------------------------- #
# 其他工具
# ---------------------------------------------------------------- #
python-dotenv>=1.0.0
requests>=2.31.0

# ---------------------------------------------------------------- #
# 開發與測試（選用）
# ---------------------------------------------------------------- #
pytest>=7.4.0
